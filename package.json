{
  "name": "esbuild-wasm-treeshake-repro",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "esbuild.ts",
  "scripts": {
    "test": "deno run -A esbuild.ts",
    "start": "deno run -A esbuild.ts",
    "install-deno": "(curl -fsSL https://deno.land/x/install/install.sh | sh) && export DENO_INSTALL=\"/home/node/.deno\" && export PATH=\"$DENO_INSTALL/bin:$PATH\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "esbuild": "^0.17.19",
    "parse-package-name": "^1.0.0",
    "resolve.exports": "^2.0.2"
  }
}
